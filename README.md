подумать над ошибками в апликейшене - какие ошибки можгут быть у бека
как должны выглядеть dto в notesService.java - прописать их 

```Но в MVVM/clean-стиле вариант с void + exceptions — нормальный и распространённый: успех = метод завершился.```



ViewModel общается с Application только так:
вызывает методы NotesService (port.in)
получает возврат (DTO/void)
либо ловит исключение (ошибка)

Других “каналов” у неё нет, и это нормально.
